# AIデイリー — 記事自動生成エージェント向けガイド

このリポジトリは、AIニュースを毎朝自動配信する静的サイト「AIデイリー」。
スケジュール実行されるエージェントは、以下のルールに従って記事を作成しpushすること。

## 毎日のタスク(デイリー記事)

1. Web検索で**過去24時間**のAIニュースを日本語・英語の両方で収集する(OpenAI、Anthropic、Google、Meta、NVIDIA、中国勢、規制・政策、日本国内の動きなど)。
2. 重要なものを**3〜5本**選ぶ。基準: 一般の読者の生活・仕事に影響しうるか、業界の大きな流れを示すか。
3. `articles/YYYY-MM-DD-daily.md` を作成する(日付は日本時間の今日)。既に同名ファイルがあれば作業不要(二重実行防止)。
4. コミットメッセージは `記事: YYYY-MM-DD 今日のAIニュース` として main に push する。ビルドと公開・SNS告知はGitHub Actionsが自動で行うので、push以外の作業は不要。

## 日曜日の追加タスク(週間まとめ)

日曜はデイリー記事に加えて `articles/YYYY-MM-DD-weekly.md`(category: news)を作成する。その週のデイリー記事(`articles/`内の過去7日分)を読み直し、「今週の大きな流れ」を3つ程度に整理して1本にまとめる。

## 週1回の追加タスク(水曜)

水曜はデイリー記事に加えて、常設記事(category: guide または tools)を1本作成する。テーマ例: AIツールの使い方、用語解説、比較記事、活用アイデア。既存記事と重複しないこと。

## 記事フォーマット

```markdown
---
title: 記事タイトル(30〜60文字、内容が具体的にわかるように)
date: YYYY-MM-DD
category: news | guide | tools
description: 検索結果やSNSに表示される説明文(80〜120文字)
tags: カンマ区切りのタグ
---

本文(Markdown)
```

デイリー記事の本文構成:
- 冒頭に1〜2文のリード
- トピックごとに `## 見出し` を立て、**「何が起きた?」「なぜ重要?」「ひとこと解説(専門用語の説明)」**の型で書く
- 末尾に `## まとめ` と、`**出典:**` としてソースURLのリスト

## 文体・品質ルール(必須)

- です・ます調。初心者に語りかけるやさしい日本語。煽らない。
- 専門用語には必ず短い解説を添える。
- **事実と推測を区別する**。未確認の報道は「未確認情報」「報道ベース」と明示する。
- 出典リンクを必ず記載する。**ソース記事の文章をコピーせず、自分の言葉で要約する**(引用する場合も1箇所・短文まで)。
- 誇大な収益・投資の勧誘的表現は書かない。特定銘柄の売買推奨は禁止。
- 1記事1,000〜2,500字程度。

## してはいけないこと

- `config/`、`pages/`、`src/`、`scripts/`、`.github/` を変更しない(記事の追加のみ行う)。
- 既存記事の書き換え・削除をしない(明示的に依頼された場合を除く)。
- アフィリエイトリンクや広告コードを記事本文に直接書かない(広告はテンプレートが自動挿入する)。

## ローカル開発(人間・対話セッション向け)

- ビルド: `npm run build` → `dist/` に出力
- プレビュー: `node scripts/serve.mjs` → http://localhost:4500
- 公開: mainにpushするとGitHub ActionsがビルドしてGitHub Pagesへデプロイする
- SNS告知(push後にローカルで実行。.secrets.json未設定ならスキップされる):
  - Bluesky: `node scripts/post-bluesky.mjs articles/<ファイル名>`
  - Threads: `node scripts/post-threads.mjs articles/<ファイル名>` (トークン延長は自動)
